import React, { useEffect, useRef, useCallback, useState } from 'react';
import MainListingsTable from './MainListingsTable';

const Listings: React.FC = () => {
    return <MainListingsTable />;
};

export default Listings;
